[project]
name = "server"
version = "0.1.0"
description = ""
authors = [{ name = "Ayan Bandyopadhyay", email = "founders@finic.ai" }]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
playwright = "^1.61.0"
fastapi = "^0.140.1"
uvicorn = "^0.51.0"
requests = "^2.34.2"
websockets = "^16.1"

[tool.poetry.scripts]
start = "server:start"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
